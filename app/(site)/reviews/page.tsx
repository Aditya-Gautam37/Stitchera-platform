import { ComingSoon } from "@/components/site/coming-soon";

export default function ReviewsPage() {
  return (
    <ComingSoon
      title="Reviews"
      phase="Phase C"
      description="Role-aware reviews for customers, tailors, and delivery partners — needs a reviews/ratings system built first, which doesn't exist in the database yet."
    />
  );
}
